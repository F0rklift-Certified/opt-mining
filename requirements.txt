pandas==2.2.2
requests==2.32.3

# Task 1 — Wind Resource (Global Wind Atlas GeoTIFF handling)
# Bumped from 1.4.3 to 1.4.4 (same 1.4.x line): Python 3.14 has no 1.4.3 wheel
# and there is no local gdal-config to build from source. 1.4.4 ships a 3.14 wheel.
rasterio==1.4.4
numpy==2.2.6

# Sprint 1 — Grid generation and feature layers (S1-02 onwards)
# Introduced at S1-02 (Finalise Common Analysis Cell) because generating ~47k
# polygon cells with attributes and writing to GeoPackage is exactly what
# GeoPandas is designed for. All downstream feature-layer tasks (S1-03–S1-08)
# will use these for spatial joins, overlay, and GeoPackage I/O.
geopandas==1.1.4
shapely==2.1.2
pyogrio==0.13.0
pyproj==3.7.2

# Test-only: property-based testing for feature-layer stages (S1-03 onwards).
# rasterstats deliberately NOT used — zonal statistics are implemented in-repo
# so the pixel-inclusion rule stays explicit and auditable.
hypothesis==6.112.1

# S1-07 — Exclusion layer: configurable rules loaded from YAML, not hard-coded
pyyaml==6.0.2

# S1-05 — Infrastructure features: AEMO KCI connection points ship as .xlsx,
# read via pandas.read_excel which requires openpyxl as the engine.
openpyxl==3.1.5
