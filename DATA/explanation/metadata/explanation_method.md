# Site Explanations — Method (S2-06a + S2-06b)

*Generated by `pipeline.explanation.run` on 2026-09-14T01:54:39+00:00. Do not edit by hand.*


This is a **deterministic, template/rule-based** explanation of every site — eligible and excluded — with **no language model involved**. The same Scored_Table, integrated table and templates always produce byte-identical explanations, which is more auditable than a generative model and cannot invent a factor the data does not support. Eligible cells are explained by their strongest factors and weaknesses (S2-06a); excluded cells state their machine- and human-readable exclusion reason(s) (S2-06b). Every record — both paths — also carries any **proxy** caveat and the cell's **data-quality / confidence** note (S2-06b, AC7).

## 1. How factors are chosen

For every **eligible** cell:

- **Positive factors** are the criteria contributing the most to the cell's S2-05 suitability score, ranked by the persisted `contrib_{feature}` value (the score is **never** recomputed here), at most 3. Only criteria the cell scores favourably on (normalised value above 0.5) are offered as strengths.
- **Weaknesses** are the criteria the cell scores poorly on (normalised value at or below 0.5), worst first, at most 2.
- A **qualitative band** is derived from the cell's normalised value for the criterion and appended to the phrase. Because the Scored_Table does not persist the normalised intermediates, they are **recomputed** from the integrated table using the scoring stage's own normaliser (`pipeline.scoring.normalise`) with the same weights, and a reconciliation guard asserts the recomputed contributions reproduce the persisted ones within the shared tolerance before any explanation is written.

## 2. Qualitative bands

| Band | Minimum normalised value |
|------|--------------------------:|
| top decile | 0.9 |
| strong | 0.65 |
| moderate | 0.35 |
| limited | 0 |

Boolean criteria use the labels `present` / `absent` rather than a decile band. A criterion that is constant over the eligible population carries no discriminating information and is never surfaced as a factor.

## 3. Screening-level language

Every headline reads: *"Higher-ranked candidate under the selected assumptions and criteria"*. This is a strategic screening output — it indicates a **higher-ranked candidate under the selected assumptions and criteria**, never a "best" or "optimal" site. Non-screening superlatives are rejected when the templates load and asserted absent by validation.

## 4. Templates (phrasing as data)

Phrasing is **user input** loaded at runtime from `pipeline/explanation/explanation_templates.yaml` (version `1.1`, SHA-256 `1b80567cab9b68bb06de1aba4bed257c1e08d16223ebcee0c8a5ecef432bc2ba`). No phrase or band label appears in `pipeline/explanation/` source.

| Criterion | Positive phrase | Weakness phrase |
|-----------|-----------------|-----------------|
| `wind_speed` | Strong wind resource | Weaker wind resource |
| `dist_transmission_km` | Close to transmission | Distant from transmission |
| `demand_proxy` | Near electrical demand | Far from electrical demand |
| `dist_substation_km` | Close to a substation | Distant from a substation |
| `slope_deg` | Gentle terrain | Steeper terrain |
| `inside_rez` | Inside a Renewable Energy Zone | Outside any Renewable Energy Zone |

## 4b. Excluded cells and caveats (S2-06b)

- **Excluded cells** carry their machine- and human-readable exclusion reason(s) — a JSON list of `{code, text}` pairs from the integrated table's `exclusion_reasons` (Decision-Engine Spec §6.5, frozen decision F16), carried through verbatim in rule-config order. Eligibility and scores are **not** recomputed here.
- **Proxy caveats** flag any proxy variable a cell used, so a proxy is never read as a direct measurement. A criterion is a proxy when the templates mark it `proxy: true`; the caveat is surfaced only when the cell had a value for that criterion. The MVP demand feature is a spatial proxy allocated below the AEMO region, not measured local demand.
- **Data-quality notes** always surface the cell's S1-09 composite confidence level (including `high`), appending the reduced-confidence reasons from `confidence_notes` when present. Exactly one note per record.

## 5. What was explained

| Measure | Cells |
|---------|------:|
| Eligible cells in the Scored_Table | 1,233 |
| Excluded cells in the integrated table | 46,078 |
| **Explained (both paths)** | **47,311** |
| Excluded cells explained | 46,078 |
| Eligible records with at least one weakness | 906 |
| Records carrying a proxy caveat | 40,674 |

## 6. Inputs, outputs and reproduction

| Input | Path | Detail |
|-------|------|--------|
| Scored_Table (S2-05) | `DATA/scoring/optmining_suitability-score_2026_nsw.gpkg` | layer `suitability_score`, SHA-256 `e2f348f652a31fc6b1b1acc1b070adcbfadd468dc37f8644e6859fe5735dd773` |
| Integrated table (S1-08) | `DATA/integration/optmining_integrated-features_2026_nsw.gpkg` | read only to recompute normalisation bounds |
| Criteria weights | `pipeline/scoring/scoring_weights.yaml` | version `1.0`, SHA-256 `d20abaef361f2a1c4c7bf251e8d6dadf189a0111526ea7d245d898db04b13f7f` |
| Explanation templates | `pipeline/explanation/explanation_templates.yaml` | version `1.1`, SHA-256 `1b80567cab9b68bb06de1aba4bed257c1e08d16223ebcee0c8a5ecef432bc2ba` |

| Output | Path |
|--------|------|
| Explanations (JSON) | `DATA/explanation/optmining_site-explanations_2026_nsw.json` |
| Explanations (CSV) | `DATA/explanation/optmining_site-explanations_2026_nsw.csv` |
| Schema | `DATA/explanation/metadata/explanation_schema.md` |
| Method report | `DATA/explanation/metadata/explanation_method.md` |
| Validation report | `DATA/explanation/metadata/explanation_validation.md` |
| Manifest | `DATA/explanation/metadata/explanation_manifest.json` |
| Provenance | `DATA/explanation/DATA_PROVENANCE.md` |
| Source register | `DATA/explanation/metadata/source_register.csv` |

- **Regenerable:** yes — `python -m pipeline --only explanation` (after `scoring`). Reproducible from the Scored_Table, the integrated table and the two config files alone, with no manual editing.
- **Validation:** 10/10 checks passed (0 failures).
- **Runtime:** 4.3 s. **Generated (UTC):** 2026-09-14T01:54:35+00:00. **Git commit:** `96ab911c2fff93617f33ffefb6db3875c43af97e-dirty`.

---

*This is a strategic screening output. It indicates where to look next; it is not a site approval, an engineering assessment or a bankable figure.*
